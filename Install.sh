# To install manually:
#Install cpanm:

wget -O - http://cpanmin.us | perl - --self-upgrade --sudo

@ install all modules
sudo cpanm  --installdeps 

#Or do this Manual install of Perl Modules, by hand

#Install Plack, a Perl web server:
#    cpanm Plack --sudo
#    cpanm Plack::Builder --sudo
    
#Install PSGI, an interface between web apps
#    cpanm PSGI --sudo
#    cpanm CGI::PSGI --sudo
    
#Install Template Toolkit
#    cpanm Toolkit --sudo
    
#Install cpanm  UUID::Tiny
#    cpanm  UUID::Tiny --sudo
    
# Install UserAgent to fetch web pages
#    cpanm LWP::UserAgent --sudo






